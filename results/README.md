# Results boundary

- `public/` is reserved for results generated from the pinned CC0 OpenNeuro
  subset. It contains no numerical outputs until the public pipeline completes.
- `restricted/` documents controlled-access provenance and contains only
  previously approved cohort aggregates, never participant-level data.
